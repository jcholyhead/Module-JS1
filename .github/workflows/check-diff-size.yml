name: Check some work has happened and linting is set up

on:
  pull_request:
    branches:
      - "main"

jobs:
  check-prettier:
    name: Check Prettier Formatting
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'

    - name: Install Prettier
      run: npm install --global prettier

    - name: Run Prettier Check
      id: prettier_check
      run: |
        OUTPUT=$(prettier --list-different . 2>&1)
        echo "Prettier Output: $OUTPUT"
        echo "OUTPUT=$OUTPUT" >> $GITHUB_ENV
      continue-on-error: true

    - name: Evaluate Prettier Output and Set Draft if Failed
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        if [[ -n "$OUTPUT" ]]; then
          COMMENT_BODY="Prettier found files that aren't formatted correctly."
          curl -s \
          -X PATCH \
          -H "Authorization: token $GITHUB_TOKEN" \
          -H "Accept: application/vnd.github.v3+json" \
          -d '{"draft": true}' \
          "https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}"
        else
          COMMENT_BODY="Prettier didn't run."
        fi
        curl -s \
        -H "Authorization: token $GITHUB_TOKEN" \
        -H "Accept: application/vnd.github.v3+json" \
        -X POST \
        -d "{\"body\": \"$COMMENT_BODY\"}" \
        "https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments"

  check-diff:
    name: Check diff size
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: "Check that diff isn't too small"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          FILES_CHANGED=$(gh pr diff ${{github.event.pull_request.number}} --name-only | wc -l)
          COMMENT_BODY="Number of files changed in this PR: ${FILES_CHANGED}"
          
          if [ $FILES_CHANGED -ge 6 ]; then
            COMMENT_BODY="🧪 ✅ Number of files changed in this PR: **${FILES_CHANGED}**.\n\n You've made a fair number of changes - keep it up! 😎"
          else
            COMMENT_BODY="🧪 ❌ Number of files changed in this PR: **${FILES_CHANGED}**. \n\n Looks like you've not answered some of the exercises."

          fi

          curl -s \
          -H "Authorization: token $GITHUB_TOKEN" \
          -H "Accept: application/vnd.github.v3+json" \
          -X POST \
          -d "{\"body\": \"$COMMENT_BODY\"}" \
          "https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments"
