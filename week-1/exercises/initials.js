let firstName = "Creola";
let middleName = "Katherine";
let lastName = "Johnson";

// Declare a variable called initials that stores the first characters from each string in upper case to represent the user's initials
// Log the variable in each case
